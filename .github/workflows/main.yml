name: Minute-by-Minute Workflow

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Init
        run: |
          echo "Starting..."

      - name: Building
        run: |
          echo "Building the project..."
