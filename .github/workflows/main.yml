name: Per Minute Workflow

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: main
      - name: Init
        run: |
          echo "Starting..."

      - name: Building
        run: |
          echo "Building the project..."
